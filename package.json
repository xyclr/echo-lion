{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.8"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "login": "npm login --registry=https://registry.npmjs.org",
    "publish": "lerna publish"
  }
}
